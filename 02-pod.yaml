apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec: 
  containers:
  - name: nginx
    image: nginx

# kind: Pod
# apiVersion: v1
# metadata:
#   name: nginx
# spec:
#   containers:
#   - name: nginx
#     image: nginx:stable-otel